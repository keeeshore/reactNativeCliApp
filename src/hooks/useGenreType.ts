import React, {useState, useEffect, useContext, ReactFragment, ChangeEvent} from 'react';

export const useGenreType = (props: {}) => {
  const getGenreLabel = (genre_id: number) => {};
  return {};
};
